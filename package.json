{
  "name": "exemplow3css",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
